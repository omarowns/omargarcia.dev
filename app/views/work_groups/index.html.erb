<h1>Work Groups</h1>
<%= render Admin::TableComponent.new(records: @work_groups, model: WorkGroup) do |c| %>
  <%= c.new_link do %>
    <%= link_to 'New Work Group', [:new, :work_group] %>
  <% end %>
<% end %>
