<%= form_with(model: work_group) do |form| %>
  <% if work_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(work_group.errors.count, "error") %> prohibited this work_group from being saved:</h2>

      <ul>
        <% work_group.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <%= render Admin::BelongsTo::ProfileComponent.new(form: form, model: work_group) %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
