<p id="notice"><%= notice %></p>

<h1>Interests</h1>
<%= render Admin::TableComponent.new(records: @interests, model: Interest) do |c| %>
  <%= c.new_link do %>
    <%= link_to 'New Interest', [:new, :interest_group, :interest] %>
  <% end %>
<% end %>
