<%= form_with(model: [interest_group, interest]) do |form| %>
  <% if interest.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(interest.errors.count, "error") %> prohibited this interest from being saved:</h2>

      <ul>
        <% interest.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :value %>
    <%= form.text_field :value %>
  </div>

  <div class="field">
    <%= form.label :profile_position %>
    <%= form.number_field :profile_position %>
  </div>

  <div class="field">
    <%= form.label :interest_group_id %>
    <%= form.text_field :interest_group_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
