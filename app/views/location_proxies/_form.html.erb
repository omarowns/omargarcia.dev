<%= form_with(model: location_proxy) do |form| %>
  <% if location_proxy.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(location_proxy.errors.count, "error") %> prohibited this location_proxy from being saved:</h2>

      <ul>
        <% location_proxy.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :position %>
    <%= form.number_field :position %>
  </div>

  <%= render Forms::LocatableComponent.new(form: form) %>

  <%= render Forms::BelongsToComponent.new(
        form: form,
        model: location_proxy,
        attribute: :location_id,
        select_model: Location,
        select_attrs: [:value, :id]
      )
  %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
