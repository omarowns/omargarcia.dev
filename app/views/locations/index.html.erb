<p id="notice"><%= notice %></p>

<h1>Locations</h1>

<%= render Admin::TableComponent.new(records: @locations, model: Location) do |c| %>
  <%= c.new_link do %>
    <%= link_to 'New Location', new_location_path %>
  <% end %>
<% end %>
