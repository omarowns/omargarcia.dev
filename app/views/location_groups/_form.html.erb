<%= form_with(model: location_group) do |form| %>
  <% if location_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(location_group.errors.count, "error") %> prohibited this location_group from being saved:</h2>

      <ul>
        <% location_group.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <%= render Forms::BelongsTo::ProfileComponent.new(form: form, model: location_group) %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
