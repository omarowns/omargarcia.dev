<h1>Profile <%= @profile.id %></h1>
<%= render Admin::ArticleComponent.new do |c| %>
  <% c.section do %>
    <p id="notice"><%= notice %></p>

    <p>
      <strong>Type:</strong>
      <%= @profile.type %>
    </p>

    <p>
      <strong>Name:</strong>
      <%= @profile.name %>
    </p>

    <p>
      <strong>Title:</strong>
      <%= @profile.title %>
    </p>

    <p>
      <strong>Animoji:</strong>
      <%= @profile.animoji %>
    </p>
  <% end %>
  <% c.footer do %>
    <%= link_to 'Edit', edit_profile_path(@profile) %>
    <%= link_to 'Back', :profiles %>
  <% end %>
<% end %>

<%= render Admin::HasMany::ImagesComponent.new(model: @profile) %>
