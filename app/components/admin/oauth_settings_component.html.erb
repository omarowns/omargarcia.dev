<% if user_has_provider_setup? %>
  <%= link_to "#{provider_name} Oauth Settings", provider_oauth_settings_url, target: '_blank' %>
<% else %>
  <%= button_to "Connect with #{OmniAuth::Utils.camelize(@provider)}", omniauth_authorization_path %>
<% end %>
