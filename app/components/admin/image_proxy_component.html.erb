<section>
  <h2>Image Proxies</h2>
  <table>
    <thead>
      <tr>
        <th>Position</th>
        <th>Imageable</th>
        <th colspan="3"></th>
      </tr>
    </thead>

    <tbody>
      <% @record.image_proxies.each do |record| %>
        <tr>
          <td><%= record.position %></td>
          <td><%= link_to record.polymorphic_title, record.imageable %></td>
          <td><%= link_to 'Show', record %></td>
          <td><%= link_to 'Edit', [:edit, record] %></td>
          <td><%= link_to 'Destroy', record, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        </tr>
      <% end %>
    </tbody>
  </table>

</section>
